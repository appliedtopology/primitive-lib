package $packageName;

#set ($type = $templateTypes.get(0))
#set ($className = $utility.getAnnotatedClassName("AbstractMatrix", $templateTypes, $genericTypes))
#set ($entryClassName = $utility.getAnnotatedClassName("MatrixEntry", $templateTypes, $genericTypes))
#set ($vectorClassName = $utility.getAnnotatedClassName("AbstractVector", $templateTypes, $genericTypes))

#if ($type == "boolean")

#include("templates/matrix/boolean/BooleanAbstractMatrix.java");
#stop

#end

public interface $className extends Iterable<$entryClassName> {
	public abstract $className like(int rows, int columns);
	
	public abstract int getNumRows();
	public abstract int getNumColumns();
	
	public abstract $type get(int row, int column);
	public abstract void set(int row, int column, $type value);
	
	#if ($utility.isPrimitive($type))
	public abstract $vectorClassName multiply($vectorClassName vector);
	#end
	
	public abstract String toString();
}